spring:
  config:
    name: OnlineSchoolKursach
  datasource:
    url: jdbc:postgresql://localhost:5432/OnlineSchoolWeb?connectTimeout=60&socketTimeout=60&tcpKeepAlive=true&ApplicationName=OnlineSchoolKursach
    username: postgres
    password: 1
    driver-class-name: org.postgresql.Driver
    hikari:
      connection-timeout: 60000
      idle-timeout: 600000
      max-lifetime: 1800000
      minimum-idle: 2
      maximum-pool-size: 10
      pool-name: HikariPool-1
      connection-test-query: SELECT 1
      leak-detection-threshold: 60000
      validation-timeout: 5000
      keepalive-time: 30000
      initialization-fail-timeout: 1
      register-mbeans: true
  jpa:
    hibernate:
      ddl-auto: none
    database: postgresql
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        connection:
          provider_disables_autocommit: false
        jdbc:
          time_zone: UTC
          batch_size: 20
        dialect: org.hibernate.dialect.PostgreSQLDialect
        order_inserts: true
        order_updates: true
        # Отключаем автоматическое закрытие соединений во время DDL операций
        temp.use_jdbc_metadata_defaults: false
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  jackson:
    serialization:
      fail-on-empty-beans: false
  mvc:
    throw-exception-if-no-handler-found: true
  web:
    resources:
      add-mappings: true
      static-locations: classpath:/static/
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:k6117039@gmail.com}
    password: ${MAIL_PASSWORD:tckbbbnshwxpwkzy}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          debug: true
        debug: true

server:
  port: 8080

jwt:
  secret: myVerySecretKeyThatIsLongEnoughForJWTAlgorithmSecurityRequirements123456789
  expiration: 86400

logging:
  level:
    com.example.OnlineSchoolKursach: DEBUG
    org.springframework.security: DEBUG
    org.springframework.mail: DEBUG
    jakarta.mail: DEBUG
    org.eclipse.angus.mail: DEBUG
    com.sun.mail: DEBUG

file:
  storage-type: minio
  max-file-size: 10MB
  max-request-size: 10MB

minio:
  endpoint: http://localhost:9000
  access-key: minioadmin
  secret-key: minioadmin
  bucket-name: school-files
  secure: false

springdoc:
  packages-to-scan: com.example.OnlineSchoolKursach
  paths-to-match: /v1/api/**
  show-actuator: true
  swagger-ui:
    operations-sorter: alpha
    tags-sorter: alpha

management:
  server:
    port: 8080
  endpoints:
    web:
      exposure:
        include: "*"
      base-path: /actuator
  endpoint:
    prometheus:
      enabled: true
  metrics:
    export:
      prometheus:
        enabled: true
