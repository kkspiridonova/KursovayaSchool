# SupSchool - Онлайн платформа для обучения
Цифровая платформа для онлайн-обучения с поддержкой курсов, уроков, заданий, оценок и сертификатов.

# Содержание

- Архитектура приложения
- Роли пользователей
- Локальный запуск
- Запуск в Docker
- Команды Docker

# Архитектура приложения

Приложение построено по многослойной архитектуре:

- **Presentation Layer (Слой представления)**
  - Controllers - обработка HTTP запросов
  - Thymeleaf Templates - веб-интерфейс

- **Business Layer (Бизнес-слой)**
  - Services - бизнес-логика приложения
  - Утилиты и вспомогательные классы

- **Data Access Layer (Слой доступа к данным)**
  - Repositories - работа с базой данных через JPA
  - Models - JPA сущности, представляющие таблицы БД

- **Infrastructure Layer (Инфраструктурный слой)**
  - PostgreSQL - база данных
  - MinIO - хранилище файлов
  - Spring Security - безопасность и аутентификация
  - JWT - токены для авторизации

## Структура пакетов

- `config/` - конфигурационные классы (SecurityConfig, MinioConfig и др.)
- `controller/` - REST и Web контроллеры (AdminController, AuthController, WebController и др.)
- `service/` - бизнес-логика (CourseService, AuthService, NotificationService и др.)
- `repository/` - доступ к данным через JPA (CourseRepository, UserRepository и др.)
- `model/` - JPA сущности (UserModel, CourseModel, RoleModel и др.)
- `security/` - безопасность (JwtAuthenticationFilter и др.)
- `util/` - утилиты (JwtUtil и др.)

# Роли пользователей

### Студент
- Просмотр доступных курсов
- Запись на курсы
- Просмотр уроков и материалов
- Выполнение заданий и отправка решений
- Просмотр оценок и комментариев
- Получение сертификатов
- Натсройка профиля
- Получение уведомлений на почту

### Преподаватель
- Создание и редактирование собственных курсов
- Создание уроков и заданий
- Просмотр решений студентов
- Выставление оценок
- Добавление комментариев

### Администратор
- Полный доступ ко всем курсам
- Управление пользователями
- Изменение ролей пользователей
- Управление категориями курсов
- Просмотр статистики по всем курсам
- Экспорт/импорт данных (CSV)
- Доступ к логам аудита

# Локальный запуск

### Требования
- Java 17
- Maven 3.6+
- PostgreSQL 15
- Docker и Docker Compose (для MinIO)
- IntelliJ IDEA

### Шаг 1: Запуск MinIO через Docker Compose

```bash
docker-compose up -d minio
```

MinIO будет доступен:
- API: http://localhost:9000
- Console: http://localhost:9001 (логин: `minioadmin`, пароль: `minioadmin`)

### Шаг 2: Создание базы данных PostgreSQL

Подключитесь к PostgreSQL и создайте базу данных:

```sql
CREATE DATABASE OnlineSchoolWeb2;
```

### Шаг 3: Выполнение SQL скрипта

Выполните скрипт из документации курсовой работы:

```bash
psql -U postgres -d OnlineSchoolWeb2 -f database_schema_final.sql
```

Или через любой SQL клиент (pgAdmin, DBeaver и т.д.) выполните содержимое файла `database_schema_final.sql`.

### Шаг 4: Настройка конфигурации (если необходимо)

Отредактируйте `src/main/resources/application.yaml` при необходимости:
- Параметры подключения к БД
- Параметры MinIO (по умолчанию уже настроены)

### Шаг 5: Запуск в IntelliJ IDEA

1. Откройте проект в IntelliJ IDEA
2. Дождитесь индексации и загрузки зависимостей Maven
3. Найдите класс `OnlineSchoolKursachApplication.java`
4. Нажмите правой кнопкой мыши → `Run 'OnlineSchoolKursachApplication'`

Или используйте кнопку запуска рядом с методом `main()`.

### Шаг 6: Проверка запуска

После запуска приложение будет доступно:
- **Веб-интерфейс:** http://localhost:8080
- **API документация (Swagger):** http://localhost:8080/swagger-ui.html

### Тестовые пользователи

После выполнения SQL скрипта будут созданы тестовые пользователи:

admin@gmail.com 123456 Администратор
teacher@gmail.com 123456 Преподаватель
student1@gmail.com 123456 Студент

### Работа с базой данных (бэкап и восстановление)

Для работы с базой данных доступны следующие .bat скрипты:

**Создание бэкапа:**
```bash
backup.bat
```
Создает бэкап базы данных в папку `backups/` с именем файла, содержащим дату и время.

**Создание дампа:**
```bash
create_dump.bat
```
Создает дамп базы данных (структура + данные) в папку `dumps/` с флагом `--clean` (включает DROP statements).

**Восстановление из бэкапа:**
```bash
restore.bat путь_к_файлу.sql
```
Пример:
```bash
restore.bat backups\backup_OnlineSchoolWeb2_20251118_014345.sql
```

**Примечания:**
- Скрипты автоматически ищут PostgreSQL в стандартных путях установки
- По умолчанию используются параметры: БД `OnlineSchoolWeb2`, пользователь `postgres`, пароль `1`, хост `localhost`, порт `5432`
- При необходимости отредактируйте переменные в начале .bat файлов

# Запуск в Docker

### Требования
- Docker 20.10+
- Docker Compose 2.0+
- Docker Desktop

### Шаг 1: Настройка переменных окружения (опционально)

Создайте файл `.env` на основе `env.example` при необходимости:

```bash
cp env.example .env
```

По умолчанию используются значения из `docker-compose.yml`.

### Шаг 2: Запуск всех сервисов

```bash
docker-compose up -d
```

Эта команда запустит:
- **PostgreSQL** на порту 5433 (с автоматической инициализацией БД из `database_schema_final.sql`)
- **MinIO** на портах 9000 (API) и 9001 (Console)
- **Spring Boot приложение** на порту 8080

### Шаг 3: Проверка статуса

```bash
docker-compose ps
```

Все сервисы должны быть в статусе `Up` и `healthy`.

### Шаг 4: Доступ к приложению

- **Веб-интерфейс:** http://localhost:8080
- **API документация:** http://localhost:8080/swagger-ui.html
- **MinIO Console:** http://localhost:9001
  - Логин: `minioadmin`
  - Пароль: `minioadmin`

### Просмотр логов

```bash
# Все сервисы
docker-compose logs -f

# Конкретный сервис
docker-compose logs -f app
docker-compose logs -f postgres
docker-compose logs -f minio
```

## 🔧 Команды Docker

### Основные команды

```bash
# Запуск всех сервисов
docker-compose up -d

# Остановка всех сервисов
docker-compose down

# Перезапуск сервиса
docker-compose restart app

# Просмотр статуса
docker-compose ps

# Просмотр логов
docker-compose logs -f [service_name]
```

### Работа с контейнерами

```bash
# Войти в контейнер приложения
docker-compose exec app sh

# Войти в контейнер PostgreSQL
docker-compose exec postgres psql -U postgres -d OnlineSchoolWeb2

# Выполнить команду в контейнере
docker-compose exec app ls -la
```

### Пересборка

```bash
# Пересобрать образ приложения
docker-compose build app

# Пересобрать и запустить
docker-compose up -d --build
```

### Очистка

```bash
# Остановка и удаление контейнеров
docker-compose down

# Удаление с volumes (удалит данные БД!)
docker-compose down -v

# Удаление неиспользуемых образов
docker image prune -a
```

### Бэкапы

```bash
# Бэкап базы данных
docker-compose exec postgres pg_dump -U postgres OnlineSchoolWeb2 > backup.sql

# Восстановление базы данных
docker-compose exec -T postgres psql -U postgres OnlineSchoolWeb2 < backup.sql
```

### Отладка

```bash
# Проверка healthcheck
docker-compose ps

# Просмотр переменных окружения
docker-compose exec app env

# Проверка подключения к БД
docker-compose exec app ping postgres
```

# Примечания

- При локальном запуске убедитесь, что MinIO запущен перед стартом приложения
- Порт PostgreSQL по умолчанию: `5433` (чтобы не конфликтовать с локальной установкой)
- Все пароли тестовых пользователей: `123456`
- SQL скрипт автоматически выполняется при первом запуске PostgreSQL в Docker

# Полезные ссылки

- **Swagger UI:** http://localhost:8080/swagger-ui.html
- **MinIO Console:** http://localhost:9001
- **Actuator Health:** http://localhost:8080/actuator/health
